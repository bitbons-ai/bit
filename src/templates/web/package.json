{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "start": "astro dev --host 0.0.0.0",
    "build": "bun run astro build",
    "preview": "bun run astro preview"
  },
  "dependencies": {
    "@astrojs/node": "{{astroNodeVersion}}",
    "astro": "{{astroVersion}}",
    "astro-icon": "{{astroIconVersion}}",
    "pocketbase": "{{pocketbaseVersion}}"
  }
}
